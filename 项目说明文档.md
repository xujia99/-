# 人脸识别视频播放系统 - 项目说明文档

## 📋 项目概述

本项目是一个基于人脸识别技术的智能视频播放系统。系统通过实时人脸识别技术，自动识别访客身份，并根据访客的历史观看记录，实现视频播放的断点续播功能。系统采用前后端分离架构，前端负责人脸识别和视频展示，后端负责数据存储和业务逻辑处理。

### 核心特性

- ✅ **实时人脸识别**：基于 `face-api.js` 实现浏览器端实时人脸检测、识别和属性分析
- ✅ **智能视频播放**：根据用户历史观看进度自动断点续播
- ✅ **数据持久化**：使用 MySQL 数据库存储用户访问记录和播放进度
- ✅ **多摄像头支持**：支持本地摄像头和网络摄像头接入
- ✅ **实时通信**：基于 WebSocket 实现前后端实时数据同步
- ✅ **隐私保护**：前端界面仅展示宣传内容，不显示用户敏感数据

---

## 🏗️ 技术架构

### 技术栈

#### 前端 (Client)
- **框架**: React 19.1.1
- **构建工具**: Vite 7.1.2
- **样式**: Tailwind CSS 3.4.17
- **人脸识别**: face-api.js 0.22.2
- **通信**: WebSocket (原生 API)

#### 后端 (Server)
- **运行时**: Node.js
- **框架**: Express 4.18.2
- **数据库**: MySQL (mysql2 3.16.1)
- **实时通信**: WebSocket (ws 8.18.3)
- **跨域处理**: CORS 2.8.5

### 系统架构图

```
┌─────────────────────────────────────────────────────────────┐
│                        前端 (React)                          │
│  ┌──────────────┐  ┌──────────────┐  ┌──────────────┐     │
│  │  摄像头输入   │  │  人脸识别引擎  │  │  视频播放器   │     │
│  │ (本地/网络)   │→ │  face-api.js  │→ │  HTML5 Video  │     │
│  └──────────────┘  └──────────────┘  └──────────────┘     │
│         │                  │                  │             │
│         └──────────────────┼──────────────────┘             │
│                            │                                │
│                    WebSocket 双向通信                        │
└────────────────────────────┼────────────────────────────────┘
                              │
                    ┌─────────▼─────────┐
                    │   后端 (Express)   │
                    │  ┌──────────────┐ │
                    │  │ WebSocket    │ │
                    │  │  服务器      │ │
                    │  └──────────────┘ │
                    │  ┌──────────────┐ │
                    │  │  REST API    │ │
                    │  │  (数据查询)   │ │
                    │  └──────────────┘ │
                    │  ┌──────────────┐ │
                    │  │  静态文件服务 │ │
                    │  │  (视频文件)   │ │
                    │  └──────────────┘ │
                    └─────────┬─────────┘
                              │
                    ┌─────────▼─────────┐
                    │   MySQL 数据库     │
                    │  ┌──────────────┐ │
                    │  │  users 表    │ │
                    │  │ (用户数据)   │ │
                    │  └──────────────┘ │
                    └───────────────────┘
```

---

## 🎯 核心功能详解

### 1. 人脸识别功能

#### 1.1 人脸检测
- **检测模型**: TinyFaceDetector (轻量级，适合实时检测)
- **检测频率**: 每 250ms 检测一次
- **检测内容**:
  - 人脸位置和边界框
  - 68 个面部关键点
  - 人脸特征描述符 (用于识别)
  - 年龄和性别
  - 面部表情 (7种情绪)

#### 1.2 人脸识别
- **识别方式**: 基于特征描述符的相似度匹配
- **匹配阈值**: 0.6 (可调整)
- **识别流程**:
  1. 提取人脸特征描述符
  2. 与已注册的人脸进行匹配
  3. 如果匹配成功，返回已有 ID
  4. 如果匹配失败，生成新访客 ID (`visitor-{count}`)

#### 1.3 人脸属性分析
- **年龄估计**: 基于深度学习模型预测
- **性别识别**: 二分类 (男性/女性)
- **表情识别**: 7种情绪 (中性、高兴、悲伤、愤怒、恐惧、厌恶、惊讶)

### 2. 视频播放功能

#### 2.1 视频文件管理
- **视频位置**: `Server/public/slides/` 目录
- **支持格式**: `.mp4`, `.webm`, `.avi`, `.mov` (不区分大小写)
- **自动发现**: 系统启动时自动查找第一个视频文件
- **URL编码**: 使用 `encodeURIComponent` 处理中文文件名

#### 2.2 断点续播机制

**播放位置保存**:
- 视频播放过程中，每 5 秒自动保存一次播放位置到数据库
- 用户离开时，立即保存当前播放位置
- 播放结束时，重置位置为 0 (循环播放)

**播放位置恢复**:
- 用户再次进入时，从数据库读取上次保存的播放位置
- 自动跳转到对应时间点继续播放
- 支持精确到秒的播放位置控制

**播放逻辑**:
```javascript
// 前端接收播放位置
if (data.slide && slideVideoRef.current) {
  const targetPosition = data.videoPosition || 0;
  videoElement.currentTime = targetPosition;
  videoElement.play();
}
```

### 3. 用户数据管理

#### 3.1 数据存储
- **存储方式**: MySQL 数据库持久化存储
- **数据表**: `users` 表
- **主要字段**:
  - `face_id`: 人脸唯一标识 (主键)
  - `visits`: 访问次数
  - `total_time`: 历史总停留时间 (秒)
  - `video_position`: 视频播放位置 (秒)
  - `last_seen`: 最后看到的时间戳
  - `enter_time`: 本次进入时间戳

#### 3.2 数据更新机制

**进入事件 (enter)**:
- 检测到新用户或已识别用户进入
- 访问次数 +1 (2秒内不重复增加)
- 记录进入时间
- 发送视频路径和播放位置

**离开事件 (leave)**:
- 用户离开摄像头范围超过 2 秒
- 计算本次停留时间并累加到总时间
- 保存当前播放位置
- 暂停视频播放

**位置更新事件 (positionUpdate)**:
- 播放过程中每 5 秒更新一次
- 离开时立即更新一次
- 播放结束时重置为 0

### 4. 摄像头管理

#### 4.1 本地摄像头
- **接入方式**: 浏览器 `getUserMedia` API
- **权限要求**: 需要用户授权摄像头权限
- **流管理**: 自动管理 MediaStream 的生命周期
- **人脸识别**: 支持完整的人脸识别功能

#### 4.2 网络摄像头
- **接入方式**: 通过 URL 直接访问视频流
- **支持格式**: HTTP/MJPEG、RTSP (需转流)、WebRTC
- **CORS处理**: 设置 `crossOrigin="anonymous"`
- **人脸识别**: 网络摄像头模式下不进行人脸识别 (技术限制)

### 5. 实时通信

#### 5.1 WebSocket 连接
- **连接地址**: `ws://localhost:5137`
- **连接管理**: 自动重连机制 (断开后 1 秒重连)
- **消息格式**: JSON 字符串

#### 5.2 消息类型

**前端 → 后端**:
```javascript
{
  faceId: "visitor-1",        // 人脸ID
  event: "enter",              // 事件类型: enter | leave | positionUpdate
  videoPosition: 120.5,        // 视频播放位置 (秒)
  currentSlide: 1              // 当前幻灯片编号
}
```

**后端 → 前端**:
```javascript
{
  slide: "http://localhost:5137/videos/xxx.mp4",  // 视频URL
  visits: 5,                   // 访问次数
  totalTime: 3600,              // 总停留时间 (秒)
  currentSlide: 1,              // 当前幻灯片
  videoPosition: 120.5,         // 播放位置 (秒)
  isVideo: true,                // 是否为视频
  message: "提示信息"           // 可选消息
}
```

---

## 📊 数据库设计

### 数据库配置
- **数据库名**: `face_recognition_db`
- **字符集**: `utf8mb4`
- **排序规则**: `utf8mb4_unicode_ci`

### 表结构: users

| 字段名 | 类型 | 说明 | 默认值 |
|--------|------|------|--------|
| `face_id` | VARCHAR(100) | 人脸唯一标识 (主键) | - |
| `visits` | INT | 访问次数 | 0 |
| `enter_time` | BIGINT | 本次进入时间戳 (毫秒) | NULL |
| `last_seen` | BIGINT | 最后看到的时间戳 (毫秒) | NULL |
| `last_increment_at` | BIGINT | 上次增加访问次数的时间戳 | 0 |
| `total_time` | INT | 历史总停留时间 (秒) | 0 |
| `current_slide` | INT | 当前幻灯片编号 | 1 |
| `video_position` | FLOAT | 视频播放位置 (秒) | 0 |
| `created_at` | TIMESTAMP | 创建时间 | CURRENT_TIMESTAMP |
| `updated_at` | TIMESTAMP | 更新时间 | CURRENT_TIMESTAMP |

### 索引
- `idx_last_seen`: 基于 `last_seen` 字段，用于查询活跃用户
- `idx_visits`: 基于 `visits` 字段，用于排序查询

---

## 📁 项目文件结构

```
人脸识别系统9.11/
├── Client/                          # 前端项目
│   ├── src/
│   │   ├── App.jsx                  # 主应用组件 (人脸识别、视频播放)
│   │   ├── FaceCard.jsx             # 人脸信息卡片组件 (已弃用)
│   │   ├── main.jsx                 # 应用入口
│   │   └── index.css                # 全局样式
│   ├── public/
│   │   └── models/                  # face-api.js 模型文件
│   │       ├── tiny_face_detector_model-*
│   │       ├── face_landmark_68_model-*
│   │       ├── face_recognition_model-*
│   │       ├── face_expression_model-*
│   │       └── age_gender_model-*
│   ├── package.json                 # 前端依赖配置
│   └── vite.config.js               # Vite 构建配置
│
├── Server/                          # 后端项目
│   ├── server.js                    # Express 服务器主文件
│   ├── db.js                        # 数据库操作封装
│   ├── db.config.js                 # 数据库连接配置
│   ├── init.sql                     # 数据库初始化脚本
│   ├── check-database.js            # 数据库检查工具
│   ├── view-data.html               # 数据查看界面
│   ├── public/
│   │   └── slides/                  # 视频文件目录
│   │       └── *.mp4                # 视频文件 (支持多种格式)
│   └── package.json                 # 后端依赖配置
│
├── start.bat                        # 一键启动脚本
└── 项目说明文档.md                   # 本文档
```

---

## 🔄 系统工作流程

### 1. 系统启动流程

```
1. 执行 start.bat
   ↓
2. 检查并安装依赖 (npm install)
   ↓
3. 启动后端服务器 (端口 5137)
   ├─ 初始化数据库连接
   ├─ 创建/检查数据库表
   └─ 查找视频文件
   ↓
4. 启动前端开发服务器 (端口 5173)
   ├─ 加载 face-api.js 模型
   └─ 建立 WebSocket 连接
   ↓
5. 系统就绪
```

### 2. 用户识别流程

```
用户进入摄像头范围
   ↓
前端每 250ms 检测一次人脸
   ↓
检测到人脸
   ├─ 提取特征描述符
   ├─ 与已注册人脸匹配
   └─ 分析年龄、性别、表情
   ↓
匹配结果
   ├─ 匹配成功 → 使用已有 face_id
   └─ 匹配失败 → 生成新 face_id (visitor-N)
   ↓
发送 enter 事件到后端
   ├─ 创建/更新用户记录
   ├─ 访问次数 +1
   ├─ 记录进入时间
   └─ 返回视频URL和播放位置
   ↓
前端接收响应
   ├─ 设置视频源
   ├─ 跳转到保存的播放位置
   └─ 开始播放视频
```

### 3. 视频播放流程

```
视频开始播放
   ↓
每 5 秒触发 onTimeUpdate
   ↓
发送 positionUpdate 事件
   ├─ faceId: 当前用户ID
   ├─ videoPosition: 当前播放位置
   └─ event: "positionUpdate"
   ↓
后端更新数据库
   └─ 保存 video_position
   ↓
用户离开 (超过 2 秒未检测到)
   ├─ 暂停视频
   ├─ 立即保存播放位置
   ├─ 发送 leave 事件
   └─ 计算并保存停留时间
   ↓
用户再次进入
   ├─ 从数据库读取 video_position
   ├─ 设置视频播放位置
   └─ 继续播放
```

### 4. 数据查询流程

```
访问 http://localhost:5137/view/view-data.html
   ↓
页面加载时调用 API
   ├─ GET /api/users → 获取所有用户
   └─ GET /api/statistics → 获取统计信息
   ↓
后端查询数据库
   ├─ SELECT * FROM users
   └─ 聚合统计查询
   ↓
返回 JSON 数据
   ↓
前端渲染表格和统计卡片
```

---

## 🔌 API 接口说明

### REST API

#### 1. 获取所有用户
```
GET /api/users

响应:
{
  "success": true,
  "data": [
    {
      "face_id": "visitor-1",
      "visits": 5,
      "total_time": 3600,
      "video_position": 120.5,
      ...
    }
  ],
  "count": 10
}
```

#### 2. 获取单个用户
```
GET /api/users/:faceId

响应:
{
  "success": true,
  "data": {
    "face_id": "visitor-1",
    "visits": 5,
    ...
  }
}
```

#### 3. 获取统计信息
```
GET /api/statistics

响应:
{
  "success": true,
  "data": {
    "summary": {
      "total_users": 10,
      "total_visits": 50,
      "total_time_all": 36000,
      "avg_visits": 5.0,
      "avg_time": 3600
    },
    "topVisits": [...],
    "topTime": [...]
  }
}
```

### WebSocket API

#### 连接地址
```
ws://localhost:5137
```

#### 消息格式

**客户端发送**:
```javascript
// 进入事件
{
  "faceId": "visitor-1",
  "event": "enter"
}

// 离开事件
{
  "faceId": "visitor-1",
  "event": "leave"
}

// 位置更新事件
{
  "faceId": "visitor-1",
  "event": "positionUpdate",
  "videoPosition": 120.5
}
```

**服务端响应**:
```javascript
{
  "slide": "http://localhost:5137/videos/xxx.mp4",
  "visits": 5,
  "totalTime": 3600,
  "currentSlide": 1,
  "videoPosition": 120.5,
  "isVideo": true,
  "message": "可选消息"
}
```

---

## 🚀 部署和使用

### 环境要求

- **Node.js**: >= 14.0.0
- **MySQL**: >= 5.7 或 >= 8.0
- **浏览器**: Chrome/Edge/Firefox (支持 WebRTC 和 WebSocket)

### 安装步骤

#### 1. 数据库配置

1. 启动 MySQL 服务
2. 修改 `Server/db.config.js` 中的数据库配置:
```javascript
module.exports = {
  host: 'localhost',
  user: 'root',
  password: '你的MySQL密码',  // 修改这里
  database: 'face_recognition_db',
  ...
};
```

3. 执行初始化脚本:
```bash
mysql -u root -p < Server/init.sql
```

或者直接在 MySQL 客户端中执行 `Server/init.sql` 文件内容。

#### 2. 安装依赖

**方式一: 使用启动脚本 (推荐)**
```bash
# Windows
start.bat

# 脚本会自动检查并安装依赖
```

**方式二: 手动安装**
```bash
# 安装后端依赖
cd Server
npm install

# 安装前端依赖
cd ../Client
npm install
```

#### 3. 准备视频文件

将视频文件放入 `Server/public/slides/` 目录:
- 支持格式: `.mp4`, `.webm`, `.avi`, `.mov`
- 系统会自动查找第一个视频文件
- 支持中文文件名 (会自动进行 URL 编码)

#### 4. 启动系统

**方式一: 使用启动脚本**
```bash
start.bat
```

**方式二: 手动启动**
```bash
# 终端1: 启动后端
cd Server
npm run serve

# 终端2: 启动前端
cd Client
npm run dev
```

#### 5. 访问系统

- **前端界面**: http://localhost:5173 (或 Vite 显示的地址)
- **数据查看**: http://localhost:5137/view/view-data.html
- **API接口**: http://localhost:5137/api/users

### 使用说明

#### 1. 摄像头设置

1. 点击"摄像头设置"面板
2. 选择摄像头类型:
   - **本地摄像头**: 使用电脑摄像头 (需要授权)
   - **网络摄像头**: 输入摄像头 URL
3. 如果选择网络摄像头，输入视频流地址 (如: `http://localhost:8080/video`)

#### 2. 人脸识别

- 站在摄像头前，系统会自动检测人脸
- 检测到人脸后，右侧视频会自动开始播放
- 系统会显示"✓ 已识别"状态

#### 3. 视频播放

- 视频会自动从上次观看的位置继续播放
- 离开摄像头范围后，视频会暂停
- 再次进入时，会从上次暂停的位置继续播放

#### 4. 查看数据

访问 `http://localhost:5137/view/view-data.html` 可以查看:
- 所有用户数据
- 访问统计信息
- 用户停留时间
- 视频播放进度

---

## 🔧 关键逻辑说明

### 1. 人脸识别稳定性机制

系统使用多个机制确保识别稳定性:

- **稳定计数**: 需要连续 4 次检测到相同人脸才确认
- **离开超时**: 超过 2 秒未检测到人脸才判定为离开
- **防重复离开**: 使用 `hasLeftRef` 防止重复触发离开事件

```javascript
// 防止重复离开的机制
if (confirmedLabelRef.current && !hasLeftRef.current && 
    Date.now() - lastSeenRef.current > LEAVE_TIMEOUT) {
  hasLeftRef.current = true; // 锁死，防止重复 leave
  // 处理离开逻辑
}
```

### 2. 播放位置同步机制

**前端保存**:
- 每 5 秒自动保存一次 (通过 `onTimeUpdate`)
- 离开时立即保存一次
- 播放结束时重置为 0

**后端存储**:
- `positionUpdate` 事件优先级最高，立即更新数据库
- 使用 `video_position` 字段存储 (FLOAT 类型，精确到秒)

**前端恢复**:
- 接收后端返回的 `videoPosition`
- 等待视频元数据加载完成 (`loadedmetadata` 事件)
- 设置 `currentTime` 并开始播放

### 3. 访问次数控制

防止短时间内重复增加访问次数:

```javascript
const MIN_INCREMENT_GAP = 2000; // 2秒
if (now - (user.last_increment_at || 0) > MIN_INCREMENT_GAP) {
  // 增加访问次数
  visits += 1;
  last_increment_at = now;
}
```

### 4. 视频文件查找逻辑

```javascript
function findVideoFile() {
  const slideDir = path.join(__dirname, "public/slides");
  const files = fs.readdirSync(slideDir);
  const videoExtensions = [".mp4", ".MP4", ".webm", ".WEBM", ...];
  return files.find(f => videoExtensions.some(ext => f.endsWith(ext)));
}
```

系统会查找第一个匹配的视频文件，支持多种格式。

### 5. URL 编码处理

处理中文文件名和特殊字符:

```javascript
const videoPath = videoFileName 
  ? `http://localhost:${PORT}/videos/${encodeURIComponent(videoFileName)}`
  : null;
```

确保视频 URL 可以正确访问包含中文的文件名。

---

## 🐛 常见问题排查

### 1. 数据库连接失败

**症状**: 后端启动时显示"数据库连接失败"

**排查步骤**:
1. 检查 MySQL 服务是否启动
2. 检查 `db.config.js` 中的密码是否正确
3. 检查数据库 `face_recognition_db` 是否已创建
4. 运行 `npm run check-db` 检查数据库状态

### 2. 视频无法播放

**症状**: 识别到人脸后视频不播放

**排查步骤**:
1. 检查 `Server/public/slides/` 目录是否有视频文件
2. 检查视频文件格式是否支持
3. 查看浏览器控制台是否有错误信息
4. 检查视频 URL 是否正确 (查看后端日志)
5. 检查文件名是否包含特殊字符 (需要 URL 编码)

### 3. 人脸识别不工作

**症状**: 摄像头画面正常，但无法识别人脸

**排查步骤**:
1. 检查模型文件是否加载成功 (查看控制台日志)
2. 检查摄像头权限是否已授权
3. 检查光照条件是否充足
4. 检查 `face-api.js` 模型文件是否完整

### 4. WebSocket 连接失败

**症状**: 前端无法连接到后端

**排查步骤**:
1. 检查后端服务器是否正常运行 (端口 5137)
2. 检查防火墙是否阻止了 WebSocket 连接
3. 查看浏览器控制台的 WebSocket 错误信息
4. 检查后端 WebSocket 服务器是否正常启动

### 5. 播放位置不准确

**症状**: 视频没有从上次位置继续播放

**排查步骤**:
1. 检查数据库中 `video_position` 字段是否有值
2. 查看后端日志，确认位置更新事件是否正常处理
3. 检查前端 `onTimeUpdate` 是否正常触发
4. 检查视频元数据是否加载完成

---

## 📝 开发说明

### 代码结构

#### 前端核心文件: `Client/src/App.jsx`

**主要功能模块**:
1. **模型加载** (第 45-55 行): 加载 face-api.js 模型
2. **摄像头管理** (第 57-93 行): 处理本地/网络摄像头切换
3. **WebSocket 通信** (第 95-146 行): 建立和维护 WebSocket 连接
4. **人脸识别循环** (第 148-312 行): 定时检测人脸并处理识别逻辑
5. **视频播放控制** (第 479-523 行): 视频元素的事件处理

**关键状态管理**:
- `faceId`: 当前识别到的人脸ID
- `confirmedLabelRef`: 已确认的人脸标签
- `hasLeftRef`: 离开状态锁 (防止重复触发)
- `lastPositionUpdateRef`: 上次位置更新时间

#### 后端核心文件: `Server/server.js`

**主要功能模块**:
1. **静态文件服务** (第 13-16 行): 提供视频文件访问
2. **REST API** (第 18-48 行): 提供数据查询接口
3. **视频文件查找** (第 50-74 行): 自动查找视频文件
4. **WebSocket 处理** (第 96-360 行): 处理实时消息和业务逻辑

**关键业务逻辑**:
- `enter` 事件: 用户进入，增加访问次数，返回视频信息
- `leave` 事件: 用户离开，计算停留时间，保存播放位置
- `positionUpdate` 事件: 更新播放位置 (优先级最高)

#### 数据库操作: `Server/db.js`

**主要函数**:
- `testConnection()`: 测试数据库连接
- `initDatabase()`: 初始化数据库表
- `getOrCreateUser(faceId)`: 获取或创建用户
- `updateUser(faceId, updates)`: 更新用户信息
- `getActiveUsers(now)`: 获取活跃用户
- `getStatistics()`: 获取统计信息

### 扩展建议

1. **多视频支持**: 修改视频查找逻辑，支持多个视频文件切换
2. **用户管理界面**: 开发完整的用户管理后台
3. **数据分析**: 添加更详细的统计分析和报表功能
4. **权限控制**: 添加管理员权限和用户权限区分
5. **日志系统**: 添加完整的操作日志记录
6. **性能优化**: 优化人脸识别频率和数据库查询性能

---

## 📄 许可证

本项目为内部使用项目，未指定许可证。

---

## 👥 维护说明

### 更新日志

- **v2.0.0**: 移除 PPT 转换功能，简化为直接视频播放
- **v1.0.0**: 初始版本，支持 PPT 转视频和人脸识别

### 技术支持

如遇到问题，请检查:
1. 本文档的"常见问题排查"部分
2. 浏览器控制台的错误信息
3. 后端服务器的日志输出
4. 数据库连接和表结构

---

**文档版本**: 1.0  
**最后更新**: 2025-01-14  
**维护者**: 开发团队

